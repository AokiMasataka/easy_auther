export type AuthInfo = {
    name: string,
    pass: string
};

export type GroupInfo = {
    name: string,
    id: string
};

export type UserInfo = {
    name: string,
    id: string
};

export type ItemInfo = {
    name: string,
    id: string
};